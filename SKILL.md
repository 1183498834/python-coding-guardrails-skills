---
name: python-coding-guardrails
description: Python 代码编写的强制质量护栏。编写、优化或重构 Python 脚本时自动应用：(1) 路径输入输出一律用 argparse 解析并指定默认路径，用 pathlib 处理；(2) 保持程序逻辑不变的前提下，优先用多线程/多进程（concurrent.futures）提速 IO 与批处理；(3) 处理文件/图片时严格内存管理，每张图片处理完释放缓存；(4) 大规模数据查找、比对时用 faiss 建索引提速；(5) 统一的防错约束：输入校验、精确异常捕获、资源释放、不硬编码路径与密钥；(6) 写完代码后立即从默认路径选取 10 个样本运行测试验证正确性，通过即完成（不先跑完整程序），未指定路径时先询问用户。当用户要求"写代码/优化代码/加速程序/多线程提速/内存管理/图片批处理/大规模向量比对/faiss/防止出错/测试验证"，或提供待编写的 Python 脚本、批处理、检索比对任务时使用。
---

# Python Coding Guardrails

写代码前先读本文件。本 Skill 只约束"怎么写"，不改变用户要求的程序行为。

## 核心原则

1. **不改变行为**：性能与健壮性优化不得改变原有程序逻辑、输出格式与结果。
2. **资源必须有界**：线程数、内存占用、批量大小、检索规模都设上限，不无限扩张。
3. **失败必须可诊断**：任何异常都要有明确错误信息与上下文，不允许静默吞掉。

## 强制检查清单（写代码前逐条核对）

### 1. CLI 与路径
- [ ] 所有输入输出路径通过 `argparse` 定义，且**每个参数都带 `default=` 默认路径**
- [ ] 用 `pathlib.Path` 处理路径，不手工拼接字符串
- [ ] 输出目录先 `mkdir(parents=True, exist_ok=True)` 再写入
- [ ] 文件读写显式指定编码（默认 `utf-8`），Windows 下注意中文路径

### 2. 并发提速
- [ ] 先判断瓶颈：IO 密集（文件/网络/图片解码）→ `ThreadPoolExecutor`；CPU 密集（计算/比对）→ `ProcessPoolExecutor`
- [ ] 保持结果顺序优先用 `executor.map()`；无需保序用 `submit()` + `as_completed()`
- [ ] `max_workers` 动态计算（如 `min(32, os.cpu_count()+4)`），不硬编码

### 3. 内存管理
- [ ] 文件/图片一律用 `with` 上下文管理器，处理完自动释放
- [ ] 图片逐张处理：处理完立即 `del` 引用并 `gc.collect()`，不在循环中累积
- [ ] 大文件流式/分块读取，禁止一次性 `read()` 全部载入

### 4. faiss 检索
- [ ] 大规模查找/比对（向量规模大或循环开销高）必须用 faiss，禁止暴力双层循环
- [ ] 向量统一 `float32`；需要余弦相似度时先归一化
- [ ] 建库与查询都批量进行；索引与原始数据用完释放

### 5. 防错
- [ ] 输入先校验（路径存在、非空、类型、数量），不校验就使用属于缺陷
- [ ] 异常精确捕获（不写裸 `except:`），`finally` 中释放资源，错误信息带文件名/上下文
- [ ] 不硬编码路径、密钥、超参数到代码中（用参数/配置/环境变量）
- [ ] 关键步骤输出日志或进度，方便定位

### 6. 测试验证（10 样本冒烟测试）
- [ ] 写完代码后**立即**运行 10 样本测试验证，**测试通过即结束交付**；不要先运行一遍完整程序再测试
- [ ] 完整程序运行仅在两种情况下执行：10 样本测试发现问题需要排查时；或使用者明确要求全量运行时
- [ ] 未指定默认路径、或默认路径无效/为空时，**停下来询问使用者**测试用哪个路径，禁止自行编造路径或跳过测试
- [ ] 10 个样本全部通过、输出符合预期才算完成（抽样与报告细节见 `references/sampling-test.md`）

### 7. 交付前自查
- [ ] 跑通最小用例（空输入、单条数据、正常数据）
- [ ] 确认未改变原逻辑与输出格式
- [ ] 确认线程、文件句柄、faiss 索引、图片内存全部释放

## 详细规则（按主题，需要时读取）

| 主题 | 文件 | 何时读 |
| --- | --- | --- |
| argparse 与路径规范、代码示例 | `references/cli-paths.md` | 涉及命令行参数或文件路径时 |
| 多线程/多进程提速、保序、安全 | `references/concurrency.md` | 涉及提速、批处理、并行时 |
| 内存管理、图片缓存释放 | `references/memory.md` | 处理图片/大文件/长循环时 |
| faiss 建索引与检索 | `references/faiss.md` | 涉及大规模查找、相似度比对时 |
| 防错检查清单与异常规范 | `references/error-safety.md` | 编写任何代码前做防御性检查时 |
| 10 样本抽样测试、询问用户路径 | `references/sampling-test.md` | 代码写完后运行测试验证时 |

每个文件都是"规则 + 正反例 + 可直接复制的模板"，按需整读，不要跳过与本任务相关的部分。
